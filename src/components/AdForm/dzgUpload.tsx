import React from 'react';
import { Upload } from 'antd';

// const DzgUpload = React.forwardRef((props: IDzgUploadProps, ref: React.Ref<React.ReactDOM>) => {
//   const { children, value, onChange, onNativeChange, ...restProps } = props;
//   if (value && !restProps.fileList) {
//     restProps.fileList = value;
//   }
//   const handleNativeChange = (info: UploadChangeParam<UploadFile<any>>) => {
//     onNativeChange && onNativeChange(info);
//     onChange && onChange(info.fileList);
//   };
//   console.log(value, 'value');

//   return (
//     <Upload onChange={handleNativeChange} {...restProps}>
//       {children}
//     </Upload>
//   );
// });

// export default DzgUpload
